spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/exo-worlds-test
    username: chrissy
    password: etisk-hacker
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
  test:
    database:
      replace: none